/* eslint-disable import/no-extraneous-dependencies, no-unused-vars */
import React from 'react';
import { render } from 'react-dom';
/* eslint-enable */

render(
  <span>[REACT APP]</span>, document.getElementById('app')
);
